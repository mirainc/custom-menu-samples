const MenuItemDescription: React.FC = ({ children }) => {
  return (
    <div className="text-2xl font-open-sans uppercase tracking-wide">
      {children}
    </div>
  );
};

export default MenuItemDescription;
